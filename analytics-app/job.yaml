apiVersion: batch/v1
kind: Job
metadata:
  name: analytics-job
spec:
  template:
    spec:
      containers:
      - name: analytics
        image: analytics-app:v1
        imagePullPolicy: Never
        args: ["python", "app.py"]
      restartPolicy: Never
  backoffLimit: 2